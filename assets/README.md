# Assets

Static media used across documentation: images, icons, screenshots, and diagrams exported as PNG/SVG.

Do not store large binary files unnecessarily. Prefer Mermaid or draw.io source files in `docs/diagrams/`.
