# Linux Fundamentals

> **Engineering Apprenticeship — Phase 00**  
> A practical Linux fundamentals tutorial from an engineering perspective.

---

## Table of Contents

1. [Linux vs Windows](#1-linux-vs-windows)
2. [Linux Filesystem Hierarchy](#2-linux-filesystem-hierarchy)
3. [Paths and Navigation](#3-paths-and-navigation)
4. [pwd, ls, and cd](#4-pwd-ls-and-cd)
5. [Creating and Managing Files and Directories](#5-creating-and-managing-files-and-directories)
6. [Reading Files](#6-reading-files)
7. [Wildcards and Pattern Matching](#7-wildcards-and-pattern-matching)
8. [Pipes and Redirection](#8-pipes-and-redirection)
9. [grep and Searching](#9-grep-and-searching)
10. [find](#10-find)
11. [File Permissions](#11-file-permissions)
12. [Users and Groups](#12-users-and-groups)
13. [Processes](#13-processes)
14. [Environment Variables](#14-environment-variables)
15. [PATH](#15-path)
16. [Package Managers](#16-package-managers)
17. [Shell Basics](#17-shell-basics)
18. [Shell Scripting Fundamentals](#18-shell-scripting-fundamentals)
19. [Exit Codes and Error Handling](#19-exit-codes-and-error-handling)
20. [Linux Troubleshooting Workflow](#20-linux-troubleshooting-workflow)

---

# 1. Linux vs Windows

Linux and Windows are both operating systems, but they expose the system to engineers in different ways.

### Windows

Windows commonly uses:

```text
C:\
├── Users\
├── Program Files\
├── Windows\
└── ...
```

PowerShell and Command Prompt are commonly used to interact with the system.

Example:

```powershell
Get-ChildItem
Get-Location
Set-Location
```

### Linux

Linux uses a single filesystem tree beginning at:

```text
/
```

Example:

```text
/
├── home
├── etc
├── usr
├── var
├── tmp
└── ...
```

A Linux shell commonly provides commands such as:

```bash
pwd
ls
cd
mkdir
cp
mv
rm
```

### Engineering perspective

The important difference is not simply:

> "Linux has different commands."

The deeper difference is that Linux exposes many operating-system concepts through the filesystem, processes, permissions, devices, configuration files, and command-line tools.

For software engineers, Linux becomes important because many servers, containers, CI systems, cloud environments, and development environments run Linux.

---

# 2. Linux Filesystem Hierarchy

Linux starts at the root directory:

```text
/
```

Everything exists somewhere below `/`.

Important directories include:

```text
/
├── bin
├── boot
├── dev
├── etc
├── home
├── lib
├── opt
├── proc
├── root
├── run
├── sbin
├── tmp
├── usr
└── var
```

## `/home`

Contains normal users' home directories.

Example:

```text
/home/justine
```

Your personal files normally live here.

---

## `/root`

The home directory of the root user.

This is different from:

```text
/
```

`/` is the filesystem root.

`/root` is the root user's home directory.

---

## `/etc`

Contains system configuration.

Examples:

```text
/etc/hosts
/etc/passwd
/etc/ssh/
```

Think:

> `/etc` = system configuration.

---

## `/var`

Contains data that changes while the system operates.

Examples include:

```text
/var/log
/var/cache
/var/lib
```

Think:

> `/var` = variable/runtime data.

---

## `/tmp`

Temporary files.

Applications may use this directory for temporary data.

---

## `/usr`

Contains many user-space programs, libraries, and other resources.

For example:

```text
/usr/bin
/usr/lib
/usr/share
```

---

## `/dev`

Represents devices exposed to the operating system.

Examples may include:

```text
/dev/null
/dev/sda
```

---

## `/proc`

A virtual filesystem containing information about processes and the running kernel.

For example:

```text
/proc/cpuinfo
/proc/meminfo
```

This demonstrates an important Linux concept:

> Some things that look like files are interfaces to operating-system information.

---

# 3. Paths and Navigation

A **path** identifies the location of something in the filesystem.

There are two major types.

## Absolute path

An absolute path starts from `/`.

Example:

```bash
/home/justine/projects
```

It describes the complete location.

---

## Relative path

A relative path starts from your current directory.

Example:

```bash
projects/app
```

Its meaning depends on where you currently are.

---

## Current directory

Linux represents the current directory as:

```text
.
```

Example:

```bash
./script.sh
```

---

## Parent directory

The parent directory is represented by:

```text
..
```

Example:

```bash
cd ..
```

This moves one directory upward.

---

# 4. pwd, ls, and cd

These are fundamental navigation commands.

## `pwd`

Print working directory.

```bash
pwd
```

Example:

```text
/home/justine/projects
```

Engineering interpretation:

> "Where am I?"

---

## `ls`

Lists directory contents.

```bash
ls
```

Useful forms:

```bash
ls -l
ls -a
ls -la
```

### `-l`

Long format.

```bash
ls -l
```

Shows information such as:

- permissions
- owner
- group
- size
- modification time
- filename

### `-a`

Includes hidden files.

Linux hidden files commonly begin with:

```text
.
```

Example:

```text
.gitignore
.bashrc
```

---

## `cd`

Change directory.

```bash
cd /home/justine
```

Go to the parent:

```bash
cd ..
```

Go home:

```bash
cd ~
```

Return to the previous directory:

```bash
cd -
```

---

# 5. Creating and Managing Files and Directories

## Create a directory

```bash
mkdir project
```

Create nested directories:

```bash
mkdir -p project/src/main
```

---

## Create an empty file

```bash
touch app.txt
```

---

## Copy

```bash
cp source.txt destination.txt
```

Copy a directory recursively:

```bash
cp -r source destination
```

---

## Move

```bash
mv old.txt new.txt
```

`mv` can also move files:

```bash
mv app.txt project/
```

---

## Remove a file

```bash
rm app.txt
```

Remove an empty directory:

```bash
rmdir directory
```

Remove a directory recursively:

```bash
rm -r directory
```

Be careful with:

```bash
rm -rf
```

It can remove files recursively without interactive confirmation.

Engineering rule:

> Treat destructive commands as irreversible until proven otherwise.

Before deleting something, inspect it.

```bash
pwd
ls
```

---

# 6. Reading Files

Linux provides several ways to inspect files.

## `cat`

Print a file:

```bash
cat file.txt
```

Good for small files.

---

## `less`

Read large files interactively:

```bash
less file.txt
```

Useful for logs and large output.

---

## `head`

Show the beginning:

```bash
head file.txt
```

Show the first 20 lines:

```bash
head -n 20 file.txt
```

---

## `tail`

Show the end:

```bash
tail file.txt
```

Follow a file as it changes:

```bash
tail -f application.log
```

This is extremely useful when troubleshooting applications.

---

# 7. Wildcards and Pattern Matching

Wildcards allow commands to work with groups of filenames.

## `*`

Matches zero or more characters.

Example:

```bash
ls *.txt
```

Matches:

```text
a.txt
notes.txt
report.txt
```

---

## `?`

Matches one character.

```bash
ls file?.txt
```

Could match:

```text
file1.txt
file2.txt
```

but not:

```text
file10.txt
```

---

## Character ranges

```bash
file[1-5].txt
```

Can match:

```text
file1.txt
file2.txt
file3.txt
file4.txt
file5.txt
```

Wildcards become especially powerful when combined with commands such as:

```bash
ls
cp
mv
rm
grep
```

---

# 8. Pipes and Redirection

One of the most important Linux concepts is connecting commands together.

## Standard output

Normally:

```bash
ls
```

prints output to the terminal.

---

## Redirect output with `>`

```bash
ls > files.txt
```

Instead of displaying the output, it writes it to `files.txt`.

Important:

```bash
>
```

overwrites the destination.

---

## Append with `>>`

```bash
ls >> files.txt
```

This adds output to the end of the file.

---

## Pipe with `|`

A pipe sends the output of one command into another command.

Example:

```bash
ls | grep ".txt"
```

Conceptually:

```text
ls
 ↓
output
 ↓
grep
 ↓
filtered output
```

This is a major engineering pattern:

> Build small tools that perform one operation and combine them.

---

## Error redirection

Standard error can be redirected:

```bash
command 2> errors.txt
```

Both standard output and standard error can be combined:

```bash
command > output.txt 2>&1
```

---

# 9. grep and Searching

`grep` searches text.

Basic example:

```bash
grep "ERROR" application.log
```

This searches for lines containing:

```text
ERROR
```

---

## Case-insensitive search

```bash
grep -i "error" application.log
```

---

## Show line numbers

```bash
grep -n "ERROR" application.log
```

---

## Recursive search

```bash
grep -r "TODO" .
```

Searches recursively from the current directory.

---

## Combine with pipes

```bash
ps aux | grep java
```

This creates a pipeline:

```text
ps
 ↓
process list
 ↓
grep
 ↓
lines containing java
```

---

# 10. find

`find` searches the filesystem.

Example:

```bash
find . -name "*.java"
```

This searches the current directory and descendants for Java files.

---

## Find directories

```bash
find . -type d
```

---

## Find files

```bash
find . -type f
```

---

## Find by name

```bash
find /var/log -name "*.log"
```

---

## Find and execute an action

Example:

```bash
find . -name "*.tmp" -delete
```

Be extremely careful with destructive operations.

A safer first step is:

```bash
find . -name "*.tmp"
```

Inspect the result before deleting anything.

---

# 11. File Permissions

Linux controls access using permissions.

A typical permission string might look like:

```text
-rwxr-xr--
```

It can be divided into:

```text
- rwx r-x r--
  │   │   │
  │   │   └── others
  │   └────── group
  └────────── owner
```

The permissions are:

```text
r = read
w = write
x = execute
```

---

## Example

```text
-rwxr-xr--
```

Means:

### Owner

```text
rwx
```

Can read, write, execute.

### Group

```text
r-x
```

Can read and execute.

### Others

```text
r--
```

Can only read.

---

## chmod

Change permissions:

```bash
chmod +x script.sh
```

This adds execute permission.

Numeric permissions are also common:

```bash
chmod 755 script.sh
```

The values represent:

```text
read    = 4
write   = 2
execute = 1
```

Therefore:

```text
7 = 4 + 2 + 1 = rwx
5 = 4 + 1     = r-x
```

So:

```text
755
```

means:

```text
owner:  rwx
group:  r-x
others: r-x
```

---

# 12. Users and Groups

Linux is a multi-user operating system.

Files have an owner and group.

Example:

```bash
ls -l
```

Might show:

```text
-rw-r--r-- 1 justine developers 1200 app.java
```

The important parts include:

```text
owner: justine
group: developers
```

This creates a security model:

```text
user
 ↓
group
 ↓
permissions
 ↓
resource access
```

---

## Current user

```bash
whoami
```

---

## User identity

```bash
id
```

This can show:

- user ID
- group ID
- groups

---

## Changing ownership

Administrative privileges may be required:

```bash
chown user file.txt
```

Change owner and group:

```bash
chown user:group file.txt
```

---