# Exercise 01B — Data or Behavior?

This exercise reinforces the distinction before we move into classes.

For each item, decide whether it is **Data** or **Behavior**.

### 1. Student

* `name`
* `study()`
* `age`
* `takeExam()`
* `grade`

### 2. Car

* `brand`
* `start()`
* `color`
* `accelerate()`
* `speed`

### 3. Dog

* `breed`
* `bark()`
* `age`
* `run()`
* `color`

### Your task

Answer like this:

```text
1. Student
name → Data
study() → Behavior
...
```

Do **all three**. After you answer, I'll check them and then we'll officially move back to **Lesson 02 → Exercise 02A**.

## Exercise 01B — Data or Behavior

### 1. Student

| Item         | Type     |
| ------------ | -------- |
| `name`       | Data     |
| `study()`    | Behavior |
| `age`        | Data     |
| `takeExam()` | Behavior |
| `grade`      | Data     |

### 2. Car

| Item           | Type     |
| -------------- | -------- |
| `brand`        | Data     |
| `start()`      | Behavior |
| `color`        | Data     |
| `accelerate()` | Behavior |
| `speed`        | Data     |

### 3. Dog

| Item     | Type     |
| -------- | -------- |
| `breed`  | Data     |
| `bark()` | Behavior |
| `age`    | Data     |
| `run()`  | Behavior |
| `color`  | Data     |

